const title = "E-commerce shop";
const description = "E-commerce shop description";

const nextSeoConfig = {
  title: title,
  description: description,
  openGraph: {
    title: title,
    description: description,
  },
};

export default nextSeoConfig;
